import csv  
with open(r"file-path", newline="") as myFile:    
  reader = csv.reader(myFile)    
  for row in reader:        
    print(row)

myFile = open(r"file-path",'w')
myFile.write('hi there, hello now\n') 
myFile.close()
